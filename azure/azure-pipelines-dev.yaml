trigger:
  branches:
    include:
      - dev

pr: none

pool:
  vmImage: ubuntu-latest

variables:
  - template: templates/variables.yaml
  - name: environment
    value: dev

stages:
  - template: templates/workflow.yaml
