apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: datahub-gms
spec:
  rules:
  - host: io-datahub-gms-dev.unitst.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: datahub-datahub-gms
            port:
              number: 8080
